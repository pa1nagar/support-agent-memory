# ============================================
# Python Dependencies for Support Agent Memory
# Backend: AWS Lambda + FastAPI
# ============================================

# Web Framework
fastapi==0.109.0
mangum==0.17.0  # AWS Lambda adapter for FastAPI
pydantic==2.5.3
pydantic-settings==2.1.0

# AWS Services
boto3==1.34.34  # AWS SDK (Bedrock, Secrets Manager, Lambda)
botocore==1.34.34

# Database
psycopg2-binary==2.9.9  # PostgreSQL driver (CockroachDB compatible)
# Note: psycopg2-binary works, but psycopg[binary] is modern alternative

# Vector support
pgvector==0.2.4  # For handling VECTOR types in Python

# HTTP Client
httpx==0.26.0  # For async HTTP requests
requests==2.31.0  # Fallback sync HTTP

# Utilities
python-dotenv==1.0.1  # Load .env files
python-json-logger==2.0.7  # Structured logging
tenacity==8.2.3  # Retry logic with exponential backoff

# Data validation
email-validator==2.1.0

# Development/Testing (optional, comment out for production Lambda)
# pytest==7.4.4
# pytest-asyncio==0.23.3
# black==24.1.1
# mypy==1.8.0
