{
  "mcpServers": {
    "cockroachdb": {
      "command": "npx",
      "args": ["-y", "@cockroachlabs/mcp-server-cockroachdb"],
      "env": {
        "COCKROACHDB_URL": "${COCKROACHDB_URL}"
      },
      "disabled": false,
      "description": "CockroachDB Managed MCP Server - set COCKROACHDB_URL in your environment"
    }
  },
  "note": "Set COCKROACHDB_URL as an environment variable before using this config. Never hardcode credentials."
}
